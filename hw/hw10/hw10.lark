rstring: "r\"" regex* "\""

?regex: character | word | group | pipe

group: "(" regex+ ")"

pipe: regex+ "|" regex+

character: LETTER | NUMBER
word: WORD

%ignore /\s+/
%import common.LETTER
%import common.NUMBER
%import common.WORD

